# TEMPLATE — replace placeholder values before applying.
# kubectl create secret generic jarvis-secrets -n jarvis \
#   --from-literal=ANTHROPIC_API_KEY=sk-ant-... \
#   --from-literal=TAVILY_API_KEY=tvly-...
apiVersion: v1
kind: Secret
metadata:
  name: jarvis-secrets
  namespace: jarvis
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "your-anthropic-api-key-here"
  TAVILY_API_KEY: "your-tavily-api-key-here"
